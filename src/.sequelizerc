const { resolve } = require('path') // Resolvo os caminhos do Linux, Windows e MAC

//configurações do sequelize
module.exports = {
    config: resolve(__dirname, 'src', 'config', 'database.js'),

    'models-path': resolve(__dirname, 'src','app','models'), 
}